def scream(words)
  words = words + "!!!!"
  return
  puts words
end

scream("Yippeee")

# it will not print anything to the screen because we used the 'return' keyword which explicitly returns the value